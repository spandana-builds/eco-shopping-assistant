// Optional Mongoose model (unused unless MongoDB is configured).
// Left as a scaffold so the project is ready to persist analyses.
//
// const mongoose = require("mongoose");
// const ProductSchema = new mongoose.Schema({
//   title: String, url: String, site: String,
//   score: Number, rating: String, analyzedAt: { type: Date, default: Date.now },
// });
// module.exports = mongoose.model("Product", ProductSchema);
module.exports = null;
